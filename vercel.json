{
  "buildCommand": "pnpm install && pnpm docs:prepare && cd website && pnpm install --frozen-lockfile --no-shamefully-hoist && pnpm build",
  "outputDirectory": "website/build",
  "devCommand": "cd website && pnpm start",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
