{
  "buildCommand": "pnpm install && pnpm docs:prepare && cd website && rm -rf node_modules && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "website/build",
  "devCommand": "cd website && pnpm start",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
